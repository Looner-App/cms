import { collections as deployCollection } from './deploy-collection';
import { collections as mintCollection } from './mint-collection';
import { collections as users } from './users';

export const collections = {
  users,
  deployCollection,
  mintCollection,
  //// todo: missing collections
  /// queueIds
  /// backend wallets
};
