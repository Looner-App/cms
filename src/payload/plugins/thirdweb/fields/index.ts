import { fields as deployCollection } from './deploy-collection';
import { fields as mintCollection } from './mint-collection';
import { fields as users } from './users';

export const fields = {
  users,
  deployCollection,
  mintCollection,
};
